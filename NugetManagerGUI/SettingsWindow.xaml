<Window x:Class="NugetManagerGUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="250" Width="520">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="NuGet Feed URL:" VerticalAlignment="Center" Width="120" />
            <TextBox x:Name="FeedUrlText" Width="360" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,6">
            <TextBlock Text="API Key:" VerticalAlignment="Center" Width="120" />
            <TextBox x:Name="ApiKeyText" Width="360" />
        </StackPanel>

        <TextBlock Grid.Row="2" Text="这些设置会被持久化到本地配置文件（AppData）并在下次启动时加载。" TextWrapping="Wrap" Foreground="Gray" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Margin="6" />
            <Button x:Name="SaveButton" Content="Save" Width="90" Margin="6" />
        </StackPanel>
    </Grid>
</Window>